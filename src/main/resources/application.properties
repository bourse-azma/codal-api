spring.application.name=codal-api
# Server
server.port=9002
server.servlet.context-path=/codal
# Redis
spring.data.redis.host=localhost
spring.data.redis.port=6379
# Cache
cache.enabled=true
cache.default-ttl-ms=3600000
cache.key-prefix=codal-api:
# Cache names
cache.names.notices=codal-notices
cache.names.notice-snapshot=codal-notice-snapshot
cache.names.companies=codal-companies
cache.names.industry-groups=codal-industry-groups
cache.names.categories=codal-categories
cache.names.financial-years=codal-financial-years
cache.names.auditors=codal-auditors
cache.names.financial-statements=codal-financial-statements
# Cache TTLs (ms)
cache.ttls.notices-ms=${VITE_CODAL_NOTICES_REFRESH_MS:900000}
# Keep the last successfully published supervisor-notice snapshot indefinitely.
# It is replaced only after a complete scheduler refresh succeeds.
cache.ttls.notice-snapshot-ms=0
cache.ttls.companies-ms=3600000
cache.ttls.industry-groups-ms=3600000
cache.ttls.categories-ms=3600000
cache.ttls.financial-years-ms=3600000
cache.ttls.auditors-ms=3600000
cache.ttls.financial-statements-ms=86400000
# External APIs
external.codal.base-url=https://search.codal.ir/api
external.codal.web-base-url=https://www.codal.ir
# One shared throttle is used by both CODAL hosts. A value of 1 prevents parallel upstream calls.
external.codal.request.max-concurrent=1
external.codal.request.min-delay-ms=1000
external.codal.request.retry-delay-ms=1000
# Latest supervisor notices snapshot.
scheduler.notices.enabled=true
scheduler.notices.refresh-ms=${VITE_CODAL_NOTICES_REFRESH_MS:900000}
scheduler.notices.initial-delay-ms=0
scheduler.notices.target-count=1000
scheduler.notices.length=12
scheduler.notices.upstream-page-size=20
# Logging
logging.level.com.ernoxin=INFO
# =========================
# Graceful Shutdown
# =========================
server.shutdown=graceful
spring.lifecycle.timeout-per-shutdown-phase=20s
spring.jmx.enabled=false
# =========================
# Virtual Threads (Java 21 / Project Loom)
# =========================
spring.threads.virtual.enabled=true
# =========================
# Tomcat Tuning
# =========================
server.tomcat.max-connections=80
server.tomcat.accept-count=20
server.tomcat.threads.max=25
server.tomcat.threads.min-spare=2
server.tomcat.connection-timeout=5s
# =========================
# Redis Connection Pool (requires commons-pool2)
# =========================
spring.data.redis.lettuce.pool.enabled=true
spring.data.redis.lettuce.pool.max-active=6
spring.data.redis.lettuce.pool.max-idle=4
spring.data.redis.lettuce.pool.min-idle=0
spring.data.redis.lettuce.pool.max-wait=2s
spring.data.redis.connect-timeout=2s
spring.data.redis.timeout=3s
